version: "3.9"

services:
  mongo:
    image: mongo:7
    container_name: pgtracker-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:

